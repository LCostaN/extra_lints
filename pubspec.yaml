name: extra_lints
description: "Extra Lint rules not included in Flutter standard ones"
version: 0.0.1
homepage:

environment:
  sdk: ^3.5.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  analyzer:
  analyzer_plugin:
  custom_lint_builder:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter: